<UserControl x:Class="Enact.Views.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Enact.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Status.id, StringFormat=ID: {0}}" Foreground="White"/>
            <TextBlock Text="{Binding Status.name, StringFormat=Name: {0}}" Foreground="White"/>
            <TextBlock Text="{Binding Status.maintenances.Count, StringFormat=Maintenances: {0}}" Foreground="White"/>
            <TextBlock Text="{Binding Status.incidents.Count, StringFormat=Incidents: {0}}" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
